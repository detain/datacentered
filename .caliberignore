# Files excluded from Caliber's doc-sync context window.
# Large binaries/logs carry no information for agent docs and only bloat context.
# (.gitignore'd paths — vendor/, workerman.log, .logs/, .opencode/ — are already
# skipped, so they do not need repeating here.)

# 1.61 MB screenshot used by the PTY docs in Web/
Web/img/phptty.png
